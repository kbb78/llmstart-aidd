# План разработки

## Прогресс

| # | Итерация | Статус |
|---|---|---|
| 1 | Каркас проекта | ✅ Готова |
| 2 | Конфиг и запуск | ✅ Готова |
| 3 | Telegram: приветствие | ✅ Готова |
| 4 | Ответ через LLM | ✅ Готова |
| 5 | История диалога | ✅ Готова |
| 6 | Адаптация роли: Онлайн-преподаватель | ✅ Готова |
| 7 | Деплой в Railway | ✅ Готова |
| 8 | Документация: README | ✅ Готова |
| 9 | Очистка истории: /clear_chat | ✅ Готова |
| 10 | Системный промпт: ИИ-преподаватель | ✅ Готова |
| 11 | Обработка фото | ✅ Готова |
| 12 | Обработка аудио | Ожидает |

---

## Итерации

### 1. Каркас проекта
> `make build` собирается без ошибок

- [x] `pyproject.toml` — пакет и зависимости
- [x] `Dockerfile` + `docker-compose.yml`
- [x] `Makefile` с целями `build / run / up / down / logs`
- [x] `.env.example` со всеми переменными
- [x] Пустой пакет `src/assistant/`

### 2. Конфиг и запуск
> Бот стартует, пишет провайдер и модель в лог. При невалидном конфиге падает с понятной ошибкой.

- [x] `Config` — читает `.env`, валидирует обязательные переменные
- [x] `__main__.py` — инициализирует логирование, собирает объекты, запускает бота

### 3. Telegram: приветствие
> Бот отвечает на `/start` и молчит на всё остальное.

- [x] `TelegramBot` — long polling, хендлер `/start`
- [x] Нетекстовые сообщения игнорируются

### 4. Ответ через LLM
> Бот отвечает на текстовые сообщения. Роль задана через `SYSTEM_PROMPT`.

- [x] `LlmClient` — вызов chat completions, один метод
- [x] `Assistant` — системный промпт + входящий текст → ответ
- [x] Хендлер текста в `TelegramBot` вызывает `Assistant`
- [x] При ошибке LLM — фиксированная фраза пользователю, исключение в лог

### 5. История диалога
> Бот помнит контекст разговора. При переполнении лимита старые реплики удаляются.

- [x] `ChatHistory` — история по `chat_id`, лимит `MAX_HISTORY_MESSAGES`
- [x] `Assistant` использует историю при каждом вызове LLM
- [x] Неуспешный вызов LLM не пишет реплику в историю

### 6. Адаптация роли: Онлайн-преподаватель
> Бот объясняет тему, отвечает на уточняющие вопросы, задаёт проверочный вопрос.

- [x] Обновить `SYSTEM_PROMPT` в `.env.example` — роль преподавателя (объяснение + уточнения + проверочный вопрос)
- [x] Обновить приветствие `/start` в `TelegramBot` — предложить выбрать тему
- [x] Обновить `docs/idea.md` и `docs/vision.md` (раздел сценариев)

### 7. Деплой в Railway
> Бот запущен в облаке Railway и доступен без локального запуска.

- [x] Добавить `railway.toml` в корень проекта
- [x] Обновить `.env.example` — пометить, что переменные задаются в дашборде Railway
- [x] Обновить `docs/vision.md` — раздел «Сборка и деплой», подраздел Railway
- [x] Выполнить деплой на Railway (GitHub-интеграция или `railway up`)
- [x] Проверить: бот отвечает в Telegram из облака

### 8. Документация: README
> README содержит всё необходимое для понимания и запуска проекта.

- [x] Обновить `README.md`: название, описание, возможности
- [x] Добавить таблицу технологического стека
- [x] Добавить инструкции по локальному запуску и деплою в Railway
- [x] Добавить раздел для скриншота / GIF с демонстрацией

### 9. Очистка истории: команда /clear_chat
> Пользователь может сбросить историю диалога командой /clear_chat.

- [x] Добавить метод `clear(chat_id)` в `ChatHistory`
- [x] Добавить хендлер `/clear_chat` в `TelegramBot`
- [x] Обновить `docs/idea.md` и `docs/vision.md`

### 10. Системный промпт: ИИ-преподаватель
> Бот ведёт полный цикл обучения в тексте: уровень и цель, программа, объяснение, задания, проверка, адаптация, прогресс.

- [x] Обновить `SYSTEM_PROMPT` в `.env.example` — узкоспециализированный преподаватель с полным циклом наставника
- [x] Обновить приветствие `/start` — предложить выбрать предмет и сформулировать цель
- [x] Обновить `README.md` — описание ИИ-преподавателя вместо разового объяснения темы

### 11. Обработка фото
> Пользователь присылает фото по теме — бот разбирает снимок и продолжает занятие.

- [x] `Config` и `.env.example` — `VISION_MODEL`, `VISION_PROMPT`; в лог старта — модель фото
- [x] `VisionClient` — байты изображения (+ подпись) → текст; OpenRouter, изображение в base64
- [x] `Assistant` — фото → текст в историю → ответ преподавателя; ошибка vision как у LLM; байты в историю не пишем
- [x] Хендлер фото в `TelegramBot` — скачать файл, передать байты в `Assistant`; стикеры по-прежнему без ответа
- [x] `__main__.py` — собрать `VisionClient` и передать в `Assistant`
- [x] Лог: входящее фото (`chat_id`, тип, без байтов) и текст после разбора
- [x] Обновить `/start` и `README.md` — можно прислать фото

### 12. Обработка аудио
> Пользователь отправляет голос или аудио — бот транскрибирует реплику и продолжает занятие.

- [ ] `Config` и `.env.example` — `AUDIO_MODEL`, `AUDIO_PROMPT`; в лог старта — модель аудио
- [ ] `AudioClient` — байты + формат → транскрипт; OpenRouter `input_audio` (base64), без Whisper; для голосовых Telegram формат `ogg`
- [ ] `Assistant` — аудио → транскрипт в историю → ответ преподавателя; ошибка как у LLM; байты в историю не пишем
- [ ] Хендлер голоса и аудио в `TelegramBot` — скачать файл, передать байты и формат в `Assistant`
- [ ] `__main__.py` — собрать `AudioClient` и передать в `Assistant`
- [ ] Лог: входящее аудио (`chat_id`, тип, без байтов) и транскрипт
- [ ] Обновить `/start` и `README.md` — можно прислать голос
